{
  "installCommand": "npm install --ignore-scripts",
  "builds": [
    {
      "src": "api/webhook.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/webhook.mjs"
    }
  ]
}
